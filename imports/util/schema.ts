import {Match} from 'meteor/check';

export {check as validate} from 'meteor/check';

export const maybe = Match.Maybe;
export const optional = Match.Optional;
export const oneOf = Match.OneOf;
export const where = Match.Where;
