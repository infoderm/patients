export const myEncodeURIComponent = (component: string): string => {
	return encodeURIComponent(component);
};

export const myDecodeURIComponent = (component: string): string => {
	return decodeURIComponent(component);
};
