name: ci:build
on:
  - push
  - pull_request
jobs:
  build:
    name: Continuous integration (build)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2

      - uses: meteor-actions/install@v1

      - name: Build 🏗️
        run: meteor build ../dist
